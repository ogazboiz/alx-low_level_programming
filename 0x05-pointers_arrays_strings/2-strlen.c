#include "main.h"
#include <string.h>

/**
 * _strlen- function that returns the length of a string
 * @s:string pointer
 * Return: return length nof the string
 */

int _strlen(char *s)
{
	return (strlen(s));
}
